import React, { useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { AlertTriangle, Bell, Building2, CalendarDays, ChevronDown, FileCheck2, LayoutDashboard, Package, Plus, Search, Truck, UserRound, Users, X } from 'lucide-react'
import './styles.css'

const peopleSeed = [
  { id: 1, firstName: 'Ahmet', lastName: 'Yılmaz', company: 'Anadolu Enerji A.Ş.', role: 'Sayaç Okuma Personeli', department: 'Saha Operasyon', city: 'Ankara', office: 'Çankaya', phone: '0532 111 22 33', email: 'ahmet@anadoluenerji.com', status: 'Aktif', documents: { myk: '2027-02-15', src: '2026-12-10', psychotechnic: '2027-01-20', license: '2028-05-10' }, vehicle: { brand: '', plate: '' } },
  { id: 2, firstName: 'Zeynep', lastName: 'Kaya', company: 'Marmara Doğalgaz Ltd.', role: 'Bölge Sorumlusu', department: 'Operasyon', city: 'İstanbul', office: 'Kadıköy', phone: '0533 222 33 44', email: '', status: 'Aktif', documents: { myk: '2027-05-10', src: '2027-05-10', psychotechnic: '2027-05-10', license: '' }, vehicle: { brand: '', plate: '' } },
  { id: 3, firstName: 'Mehmet', lastName: 'Demir', company: 'Anadolu Enerji A.Ş.', role: 'Sayaç Okuma Personeli', department: 'Saha Operasyon', city: 'Konya', office: 'Selçuklu', phone: '0534 333 44 55', email: 'mehmet@anadoluenerji.com', status: 'Ayrıldı', documents: { myk: '2026-09-27', src: '', psychotechnic: '', license: '' }, vehicle: { brand: '', plate: '34 ABC 123' } },
]
const companySeed = [
  { id: 1, name: 'Anadolu Enerji A.Ş.', sector: 'Enerji', city: 'Ankara', phone: '0312 000 00 00', email: 'info@anadoluenerji.com', status: 'Aktif' },
  { id: 2, name: 'Marmara Doğalgaz Ltd.', sector: 'Doğalgaz', city: 'İstanbul', phone: '0216 000 00 00', email: 'info@marmaradogalgaz.com', status: 'Aktif' },
]
const emptyPerson = { firstName: '', lastName: '', company: '', role: '', department: '', city: '', office: '', phone: '', email: '', status: 'Aktif', documents: { myk: '', src: '', psychotechnic: '', license: '' }, vehicle: { brand: '', plate: '' } }
const emptyCompany = { name: '', sector: '', city: '', phone: '', email: '', status: 'Aktif' }
const docs = { myk: 'MYK', src: 'SRC', psychotechnic: 'Psikoteknik', license: 'Ehliyet' }
const navItems = [['Genel Bakış', LayoutDashboard], ['Personeller', Users], ['Şirketler', Building2], ['Belgeler', FileCheck2], ['Depolar', Package], ['Araç Filosu', Truck]]
const nameOf = p => `${p.firstName || ''} ${p.lastName || ''}`.trim()
const docStatus = date => { if (!date) return 'Eksik'; const days = Math.ceil((new Date(date) - new Date()) / 86400000); return days < 0 ? 'Süresi doldu' : days <= 60 ? `${days} gün kaldı` : 'Geçerli' }

function App() {
  const [people, setPeople] = useState(() => JSON.parse(localStorage.getItem('personel360_people') || 'null') || peopleSeed)
  const [companies, setCompanies] = useState(() => JSON.parse(localStorage.getItem('personel360_companies') || 'null') || companySeed)
  const [active, setActive] = useState('Genel Bakış')
  const [query, setQuery] = useState('')
  const [personForm, setPersonForm] = useState(null)
  const [companyForm, setCompanyForm] = useState(null)
  const [detail, setDetail] = useState(null)

  const warnings = people.reduce((n, p) => n + Object.values(p.documents || {}).filter(d => docStatus(d) !== 'Geçerli').length, 0)
  const filteredPeople = useMemo(() => people.filter(p => `${nameOf(p)} ${p.company} ${p.role} ${p.city}`.toLocaleLowerCase('tr').includes(query.toLocaleLowerCase('tr'))), [people, query])
  const savePeople = next => { setPeople(next); localStorage.setItem('personel360_people', JSON.stringify(next)) }
  const saveCompanies = next => { setCompanies(next); localStorage.setItem('personel360_companies', JSON.stringify(next)) }
  const savePerson = p => { const next = p.id ? people.map(x => x.id === p.id ? p : x) : [{ ...p, id: Date.now() }, ...people]; savePeople(next); setPersonForm(null) }
  const saveCompany = c => { const next = c.id ? companies.map(x => x.id === c.id ? c : x) : [{ ...c, id: Date.now() }, ...companies]; saveCompanies(next); setCompanyForm(null) }
  const deleteCompany = id => { const c = companies.find(x => x.id === id); if (!c || !confirm(`${c.name} şirketi silinsin mi?`)) return; saveCompanies(companies.filter(x => x.id !== id)) }
  const deletePerson = id => { const p = people.find(x => x.id === id); if (p && confirm(`${nameOf(p)} silinsin mi?`)) savePeople(people.filter(x => x.id !== id)) }

  return <div className="app">
    <aside className="sidebar"><div className="brand"><div className="brand-icon"><Users size={20} /></div><div><b>Personel<span>360</span></b><small>Yönetim Sistemi</small></div></div><div className="workspace"><Building2 size={16} /><div><small>Çalışma alanı</small><strong>Merkez Yönetim</strong></div><ChevronDown size={15} /></div><small className="nav-label">MENÜ</small>{navItems.map(([label, Icon]) => <button key={label} className={`nav-item ${active === label ? 'active' : ''}`} onClick={() => setActive(label)}><Icon size={18} />{label}{label === 'Belgeler' && <em>{warnings}</em>}</button>)}<div className="account"><div className="avatar dark">FA</div><div><b>Fatih Ayka</b><small>Sistem Yöneticisi</small></div></div></aside>
    <main className="main-panel"><header><div className="breadcrumb">Yönetim <b>/</b> <strong>{active}</strong></div><div className="userbar"><button className="icon-button"><Bell size={18} /></button><div className="avatar">FA</div><span>Fatih Ayka</span><ChevronDown size={15} /></div></header><section className="content-shell">
      {active === 'Genel Bakış' && <Dashboard people={people} warnings={warnings} setActive={setActive} openPerson={() => setPersonForm({ ...emptyPerson, documents: { ...emptyPerson.documents }, vehicle: { ...emptyPerson.vehicle } })} />}
      {active === 'Personeller' && <PeoplePage people={filteredPeople} query={query} setQuery={setQuery} openPerson={() => setPersonForm({ ...emptyPerson, documents: { ...emptyPerson.documents }, vehicle: { ...emptyPerson.vehicle } })} edit={setPersonForm} detail={setDetail} remove={deletePerson} />}
      {active === 'Şirketler' && <CompaniesPage companies={companies} people={people} query={query} setQuery={setQuery} add={() => setCompanyForm({ ...emptyCompany })} edit={setCompanyForm} remove={deleteCompany} />}
      {active === 'Belgeler' && <DocumentsPage people={people} />}
      {active === 'Depolar' && <SimplePage title="Depo ve stok görünümü" eyebrow="DEPO" cards={[['Toplam Demirbaş', 16, Package, 'blue'], ['Müsait Stok', 18, Package, 'teal'], ['Zimmetli', 7, Users, 'green']]} />}
      {active === 'Araç Filosu' && <VehiclesPage people={people} />}
    </section></main>
    {personForm && <PersonModal value={personForm} close={() => setPersonForm(null)} save={savePerson} />}
    {companyForm && <CompanyModal value={companyForm} close={() => setCompanyForm(null)} save={saveCompany} />}
    {detail && <PersonDetail person={detail} close={() => setDetail(null)} />}
  </div>
}

function Dashboard({ people, warnings, setActive, openPerson }) { return <div className="page-stack"><div className="page-heading"><div><small className="eyebrow">GENEL BAKIŞ</small><h1>Gösterge Paneli</h1></div><button className="primary" onClick={openPerson}><Plus size={18} /> Yeni Personel</button></div><div className="card-grid"><Stat title="Toplam Personel" value={people.length} icon={Users} tone="teal" /><Stat title="Aktif Personel" value={people.filter(p => p.status === 'Aktif').length} icon={UserRound} tone="green" /><Stat title="Belge Uyarıları" value={warnings} icon={AlertTriangle} tone="amber" /><Stat title="Araç Sayısı" value={people.filter(p => p.vehicle?.plate).length} icon={Truck} tone="blue" /></div><div className="content-grid"><div className="panel"><h3>Son belge uyarıları</h3><div className="stack-list">{people.flatMap(p => Object.entries(p.documents || {}).filter(([, d]) => docStatus(d) !== 'Geçerli').map(([k, d]) => <div className="list-row" key={`${p.id}-${k}`}><div><strong>{nameOf(p)}</strong><small>{docs[k]}</small></div><span className="badge warn">{docStatus(d)}</span></div>))}</div></div><div className="panel"><h3>İşlem hızlı erişim</h3><div className="stack-list"><button className="mini-button" onClick={openPerson}><Plus size={16} /> Personel Ekle</button><button className="mini-button" onClick={() => setActive('Şirketler')}><Building2 size={16} /> Şirketleri Gör</button><button className="mini-button" onClick={() => setActive('Belgeler')}><CalendarDays size={16} /> Belgeleri Gör</button></div></div></div></div> }
function Stat({ title, value, icon: Icon, tone }) { return <div className="stat-card"><div className={`icon-box ${tone}`}><Icon size={18} /></div><div><small>{title}</small><strong>{value}</strong></div></div> }
function PeoplePage({ people, query, setQuery, openPerson, edit, detail, remove }) { return <div className="page-stack"><div className="toolbar-row"><div className="search-box"><Search size={16} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Ad, şirket, görev veya il ara..." /></div><button className="primary" onClick={openPerson}><Plus size={18} /> Yeni Personel</button></div><div className="panel table-panel"><table><thead><tr><th>Personel</th><th>Şirket / Görev</th><th>Bölge</th><th>Belge</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>{people.map(p => { const ds = Object.values(p.documents || {}); return <tr key={p.id}><td><strong>{nameOf(p)}</strong><small>{p.phone || 'Telefon yok'}</small></td><td><strong>{p.company || '-'}</strong><small>{p.department || p.role || '-'}</small></td><td>{p.city || '-'} / {p.office || '-'}</td><td>{ds.filter(d => docStatus(d) === 'Geçerli').length} / {ds.length}</td><td><span className={`status-badge ${p.status === 'Aktif' ? 'success' : 'warn'}`}>{p.status}</span></td><td className="action-col"><button className="simple-button" onClick={() => detail(p)}>Detay</button><button className="simple-button" onClick={() => edit(p)}>Düzenle</button><button className="simple-button danger" onClick={() => remove(p.id)}>Sil</button></td></tr>})}</tbody></table></div></div> }
function CompaniesPage({ companies, people, query, setQuery, add, edit, remove }) { const list = companies.filter(c => `${c.name} ${c.sector} ${c.city}`.toLocaleLowerCase('tr').includes(query.toLocaleLowerCase('tr'))); return <div className="page-stack"><div className="page-heading"><div><small className="eyebrow">ŞİRKET YÖNETİMİ</small><h1>Şirketler</h1></div><button className="primary" onClick={add}><Plus size={18} /> Yeni Şirket</button></div><div className="toolbar-row"><div className="search-box"><Search size={16} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Şirket, sektör veya il ara..." /></div></div><div className="panel table-panel"><table><thead><tr><th>Şirket</th><th>Sektör</th><th>İl</th><th>Personel</th><th>İletişim</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>{list.map(c => <tr key={c.id}><td><strong>{c.name}</strong></td><td>{c.sector || '-'}</td><td>{c.city || '-'}</td><td><span className="status-badge success">{people.filter(p => p.company === c.name).length} kişi</span></td><td><strong>{c.phone || '-'}</strong><small>{c.email || '-'}</small></td><td><span className={`status-badge ${c.status === 'Aktif' ? 'success' : 'warn'}`}>{c.status}</span></td><td className="action-col"><button className="simple-button" onClick={() => edit(c)}>Düzenle</button><button className="simple-button danger" onClick={() => remove(c.id)}>Sil</button></td></tr>)}</tbody></table></div></div> }
function DocumentsPage({ people }) { return <div className="page-stack"><div className="page-heading"><div><small className="eyebrow">BELGE TAKİBİ</small><h1>Belge ve geçerlilik yönetimi</h1></div></div><div className="panel table-panel"><table><thead><tr><th>Personel</th>{Object.values(docs).map(x => <th key={x}>{x}</th>)}<th>Durum</th></tr></thead><tbody>{people.map(p => <tr key={p.id}><td><strong>{nameOf(p)}</strong><small>{p.company}</small></td>{Object.keys(docs).map(k => <td key={k}>{docStatus(p.documents?.[k])}</td>)}<td><span className="status-badge warn">Takip</span></td></tr>)}</tbody></table></div></div> }
function VehiclesPage({ people }) { return <div className="page-stack"><div className="page-heading"><div><small className="eyebrow">ARAÇ</small><h1>Hizmet aracı listesi</h1></div></div><div className="panel table-panel"><table><thead><tr><th>Personel</th><th>Marka / Model</th><th>Plaka</th></tr></thead><tbody>{people.filter(p => p.vehicle?.plate).map(p => <tr key={p.id}><td>{nameOf(p)}</td><td>{p.vehicle.brand || '-'}</td><td>{p.vehicle.plate}</td></tr>)}</tbody></table></div></div> }
function SimplePage({ title, eyebrow, cards }) { return <div className="page-stack"><div className="page-heading"><div><small className="eyebrow">{eyebrow}</small><h1>{title}</h1></div></div><div className="card-grid">{cards.map(([t, v, I, tone]) => <Stat key={t} title={t} value={v} icon={I} tone={tone} />)}</div></div> }
function Field({ label, value, onChange, type = 'text', options }) { return <label className="field"><span>{label}</span>{options ? <select value={value || ''} onChange={onChange}><option value="">Seçiniz</option>{options.map(o => <option key={o}>{o}</option>)}</select> : <input type={type} value={value || ''} onChange={onChange} />}</label> }
function PersonModal({ value, close, save }) { const [f, setF] = useState(value); const u = (k, v) => setF(x => ({ ...x, [k]: v })); const n = (g, k, v) => setF(x => ({ ...x, [g]: { ...x[g], [k]: v } })); return <div className="overlay" onClick={close}><div className="modal" onClick={e => e.stopPropagation()}><button className="close-button" onClick={close}><X size={16} /></button><div className="modal-header"><h2>{f.id ? 'Personel Kartı' : 'Yeni Personel'}</h2><p>Personel bilgilerini kaydedin.</p></div><div className="modal-body"><div className="form-grid"><Field label="Ad" value={f.firstName} onChange={e => u('firstName', e.target.value)} /><Field label="Soyad" value={f.lastName} onChange={e => u('lastName', e.target.value)} /><Field label="Şirket" value={f.company} onChange={e => u('company', e.target.value)} /><Field label="Görev" value={f.role} onChange={e => u('role', e.target.value)} /><Field label="Telefon" value={f.phone} onChange={e => u('phone', e.target.value)} /><Field label="İl" value={f.city} onChange={e => u('city', e.target.value)} /><Field label="Durum" value={f.status} options={['Aktif', 'Ayrıldı']} onChange={e => u('status', e.target.value)} /><Field label="Plaka" value={f.vehicle.plate} onChange={e => n('vehicle', 'plate', e.target.value.toUpperCase())} /></div></div><div className="modal-actions"><button className="cancel-button" onClick={close}>Vazgeç</button><button className="primary" disabled={!f.firstName || !f.lastName} onClick={() => save(f)}>Kaydet</button></div></div></div> }
function CompanyModal({ value, close, save }) { const [f, setF] = useState(value); const u = (k, v) => setF(x => ({ ...x, [k]: v })); return <div className="overlay" onClick={close}><div className="modal small-modal" onClick={e => e.stopPropagation()}><button className="close-button" onClick={close}><X size={16} /></button><div className="modal-header"><h2>{f.id ? 'Şirketi Düzenle' : 'Yeni Şirket'}</h2><p>Şirket bilgilerini girin.</p></div><div className="modal-body"><div className="form-grid single"><Field label="Şirket adı" value={f.name} onChange={e => u('name', e.target.value)} /><Field label="Sektör" value={f.sector} onChange={e => u('sector', e.target.value)} /><Field label="İl" value={f.city} onChange={e => u('city', e.target.value)} /><Field label="Telefon" value={f.phone} onChange={e => u('phone', e.target.value)} /><Field label="E-posta" value={f.email} onChange={e => u('email', e.target.value)} /><Field label="Durum" value={f.status} options={['Aktif', 'Pasif']} onChange={e => u('status', e.target.value)} /></div></div><div className="modal-actions"><button className="cancel-button" onClick={close}>Vazgeç</button><button className="primary" disabled={!f.name} onClick={() => save(f)}>Kaydet</button></div></div></div> }
function PersonDetail({ person, close }) { return <div className="overlay" onClick={close}><div className="modal detail-modal" onClick={e => e.stopPropagation()}><button className="close-button" onClick={close}><X size={16} /></button><div className="detail-head"><div className="big-avatar">{nameOf(person).split(' ').map(x => x[0]).join('').slice(0, 2)}</div><div><h3>{nameOf(person)}</h3><p>{person.role} · {person.company}</p></div></div><div className="detail-grid"><div><small>Telefon</small><strong>{person.phone || '-'}</strong></div><div><small>E-posta</small><strong>{person.email || '-'}</strong></div><div><small>İl / Ofis</small><strong>{person.city || '-'} / {person.office || '-'}</strong></div><div><small>Durum</small><strong>{person.status}</strong></div></div><div className="detail-section"><h4>Belge durumu</h4><div className="doc-detail-list">{Object.entries(person.documents || {}).map(([k, v]) => <div className="doc-detail-item" key={k}><span>{docs[k]}</span><strong>{docStatus(v)}</strong></div>)}</div></div></div></div> }

createRoot(document.getElementById('root')).render(<App />)
